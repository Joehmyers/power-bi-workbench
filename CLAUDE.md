# CLAUDE.md

Claude Code reads this file automatically but does not read `AGENTS.md`
natively. The canonical, tool-agnostic agent instructions live in `AGENTS.md`;
the import below pulls them in so every tool shares one source of truth.

@AGENTS.md
